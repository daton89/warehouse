import { FlyingAddToCartDirective } from './flying-add-to-cart.directive'

describe('FlyingAddToCartDirective', () => {
  it('should create an instance', () => {
    const directive = new FlyingAddToCartDirective()
    expect(directive).toBeTruthy()
  })
})
